import { InProgress } from "@/components/in-progress";


export default function DatasetsListing() {
  return (
    <main className="flex h-full w-full flex-col items-center justify-center gap-2">
      <InProgress />
    </main>
  );
}
